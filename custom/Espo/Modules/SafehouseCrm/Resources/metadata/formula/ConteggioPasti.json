{
    "beforeSaveCustomScript": "ifThen(isNew() && isEmpty(data), data = date\\today()); ifThen(isNew() && isEmpty(foodUnitPrice), foodUnitPrice = 1.5); totalePasti = add(adulti, minori); foodCost = multiply(totalePasti, foodUnitPrice); giornoSettimana = datetime\\format(data, null, 'dddd'); name = string\\concatenate('Pasti ', data);"
}
