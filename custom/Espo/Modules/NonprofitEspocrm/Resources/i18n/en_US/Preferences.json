{
    "fields": {
        "notificationSound": "Notification Sound",
        "webPushEnabled": "Browser push notifications",
        "assignmentPushNotificationsIgnoreEntityTypeList": "In-app style push (by record type)"
    },
    "labels": {
        "webPushStatusOn": "Enabled on this device",
        "webPushStatusOff": "Disabled",
        "webPushCheckPermissions": "Check permissions",
        "webPushOpenSettings": "Open permission help",
        "webPushChecklistTitle": "Fix permissions, then try again",
        "webPushSendTest": "Send test push",
        "webPushDelaySeconds": "Delay (seconds)"
    },
    "tooltips": {
        "webPushEnabled": "Optional channel, separate from in-app (bell) and email. Only you control this for your account. Enabling registers this browser for push; in-app notifications keep working either way.",
        "webPushHelp": "Push is not the same as in-app notifications. Use “Check permissions” to verify browser/OS access. On a phone: leave the browser running in the background — do not swipe it away or force-close it; that keeps delivery reliable. A setup checklist appears only if the check fails.",
        "webPushSendTest": "Sends a real browser push to this device. Optional delay lets you minimize the window first (0 = now, max 300).",
        "assignmentPushNotificationsIgnoreEntityTypeList": "Same entity list as in-app assignment notifications. Checked types may receive browser push when an in-app notification is created for that record type. Requires browser push to be enabled above."
    },
    "messages": {
        "webPushUnsupported": "This browser does not support Web Push.",
        "webPushEnableFailed": "Could not enable browser push.",
        "webPushEnabledOk": "Browser push enabled for this device.",
        "webPushPermissionsOk": "Notification permissions look good.",
        "webPushPermissionsBad": "Permissions are not ready. Follow the checklist below.",
        "webPushTestSent": "Test push sent ({sent}). Check the OS / browser notification.",
        "webPushTestScheduled": "Test push scheduled in {seconds}s — minimize the browser and wait.",
        "webPushTestFailed": "Test push failed (no active subscription on this account). Enable push first.",
        "webPushTestStale": "Push subscription was stale and was removed. Click the checkbox again (or retry test) to re-register this browser.",
        "webPushTestSendFailed": "Test push failed (browser push service rejected delivery). Check OS/browser notification settings and retry.",
        "webPushTestInvalidDelay": "Delay must be between 0 and 300 seconds."
    }
}
